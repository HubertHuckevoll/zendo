#!/bin/bash

# Create symbolic link to coins directory (one level up)
echo "Creating symbolic link to '../coins' directory..."
ln -sf ../coins ./coins

# Build the Docker image
echo "Building Docker image 'zendo'..."
sudo docker build -t zendo -f dockerfile .

echo "Docker image 'zendo' built successfully."
